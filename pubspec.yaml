name: smart_search_list
description: >-
  Searchable list widget for Flutter. Offline filtering, async loading, pagination,
  fuzzy search, grouped lists, and multi-select. Zero dependencies.
version: 0.7.5
repository: https://github.com/fikretonuresen/smart_search_list
issue_tracker: https://github.com/fikretonuresen/smart_search_list/issues
environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

topics:
  - search
  - listview
  - filter
  - fuzzy-search
  - accessibility

flutter:
